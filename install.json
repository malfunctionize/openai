{
  "run": [{
    "method": "input",
    "params": {
      "title": "API Key",
      "type": "notify",
      "form": [{
        "key": "key",
        "description": "Enter your OpenAI API Key"
      }]
    }
  }, {
    "method": "set",
    "params": {
      "self": {
        "config.json": {
          "key": "{{input.key}}"
        }
      }
    }
  }, {
    "method": "notify",
    "params": {
      "html": "API key saved to config.json",
      "action": "close"
    }
  }]
}
